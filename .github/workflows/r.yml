name: R CI

on:
  push:

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Setup R
      uses: r-lib/actions/setup-r@v2
    
    # Install dependencies using the DESCRIPTION file in the footwrap folder
    - name: Install package dependencies
      run: |
        Rscript -e 'install.packages("remotes", repos = "http://cran.rstudio.com")'
        Rscript -e 'setwd("footwrap"); remotes::install_deps(dependencies = TRUE, repos = "http://cran.rstudio.com")'

    # Run your R script and tests
    - name: Source script and run tests
      run: |
        Rscript -e 'setwd("footwrap"); source("R/shay_script.R")'
        Rscript -e 'setwd("footwrap"); testthat::test_dir("tests/testthat")'


      # echo "Placeholder for running tests"
        # Replace the above echo command with actual test execution when ready
        # testthat::test_dir("tests/testthat")

# name: R CI

# on:
#   push:
#     branches:
#       - shayla

# jobs:
#   run-tests:
#     runs-on: ubuntu-latest
#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v2

#     - name: Setup R
#       uses: r-lib/actions/setup-r@v2

#     - name: Find R library path
#       id: r-lib-path
#       run: echo "R_LIB_PATH=$(Rscript -e 'cat(.libPaths()[1])')" >> $GITHUB_ENV

#     - name: Cache R packages
#       uses: actions/cache@v2
#       with:
#         path: ${{ env.R_LIB_PATH }}
#         key: ${{ runner.os }}-r-${{ hashFiles('**/DESCRIPTION') }}-2024-02-04
#         restore-keys: ${{ runner.os }}-r-

#     - name: Install dependencies
#       run: |
#         Rscript -e 'install.packages("remotes", repos = "http://cran.rstudio.com")'
#         Rscript -e 'remotes::install_deps(dependencies = TRUE, repos = "http://cran.rstudio.com")'
#         Rscript -e 'install.packages(c("httr", "ggplot2", "jsonlite", "tidyverse"), repos = "http://cran.rstudio.com")'

#     - name: Install the footwrap package
#       run: R CMD INSTALL --library=$R_LIB_PATH footwrap

#     - name: Run tests
#       run: Rscript -e 'testthat::test_dir("footwrap/tests/testthat")'


    # - name: Run tests
    #   run: Rscript -e 'testthat::test_dir("footwrap/tests/testthat")'
